# PRAISE first science - Phase 3 / (I1,M0)

Phase 3 starts only after Phase 2 has materialized and hash-frozen the concrete public I1 cards.

Corrected boundary:

`Phase 1 frozen A_G -> Phase 2 exact A_i + materialized/frozen I1 -> Phase 3 M0 consumes I1`

`A_i` is part of the instantiated information object `I1`. Therefore Phase 3 must not derive, choose or alter provider-local admissibility regions. M0 receives the same frozen cards that will later be supplied unchanged to M1.

The full rho support is already exposed by I1. M0 may choose which rho slice(s) to consume as part of its composition rule. For the first analytic baseline this may include the equal global-violation-budget slice `rho_i = 1-(1-rho_G)/3`, provided the required counting/alignment assumptions are explicitly validated.

No Phase-3 scientific implementation is currently frozen. The previously created `A_G -> A_i` structural code was removed from Phase 3 after clarifying that this localization belongs to Phase-2 I1 instantiation.

The next Phase-3 step is postponed until the final Phase-2 card set exists and is frozen.
